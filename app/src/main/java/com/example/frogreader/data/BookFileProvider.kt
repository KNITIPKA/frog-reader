package com.example.frogreader.data

import androidx.core.content.FileProvider

/** Grants access only to the share snapshots declared in book_share_paths.xml. */
class BookFileProvider : FileProvider()
